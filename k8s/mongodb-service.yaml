apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  labels:
    app: technova-mongodb
spec:
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: technova-mongodb
  type: ClusterIP
