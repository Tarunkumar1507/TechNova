apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: technova-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007 # Access app on http://<node-ip>:30007
  selector:
    app: technova-frontend
  type: NodePort
