apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: technova-backend
spec:
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: technova-backend
  type: ClusterIP
